# Don't block installs on package "release age" — reproducible CI/Docker builds
# must not depend on how recently a (lockfile-pinned) version was published.
# We rely on the committed pnpm-lock.yaml + PR review for supply-chain safety.
minimum-release-age=0
